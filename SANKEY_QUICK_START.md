# Sankey 流量图 - 快速使用指南

## 🚀 立即使用

### 1. 重启服务

```bash
# 重新构建（如果还没构建）
go build -o new-api

# 启动服务
./new-api
```

### 2. 访问功能

1. 登录系统: http://localhost:3000
2. 进入 Dashboard
3. 点击 **"Traffic Flow"** (或 **"流量流向"**) 标签页

### 3. 查看流量图

系统会自动显示 token 流向的 Sankey 图：

```
用户 → API Token → 用户组 → 模型 → 通道
```

## 📊 功能说明

### 基本操作

| 操作 | 说明 |
|------|------|
| **查看流量** | 线条粗细代表流量大小 |
| **点击节点** | 高亮该节点相关的所有路径 |
| **点击连接** | 高亮该连接的源和目标节点 |
| **切换指标** | Quota / Tokens / Requests |
| **筛选用户** | (管理员) 下拉选择特定用户 |
| **隐藏节点** | 取消勾选不需要的节点类型 |

### 指标类型

| 指标 | 说明 | 适用场景 |
|------|------|----------|
| **Quota** | 按配额消耗显示 | 查看成本分布 |
| **Tokens** | 按 token 数量显示 | 查看使用量 |
| **Requests** | 按请求次数显示 | 查看调用频率 |

### 权限说明

| 角色 | 可见数据 | 功能 |
|------|----------|------|
| **普通用户** | 只看自己的 token 流向 | 基本查看 |
| **管理员** | 所有用户的流向 | 用户筛选 |
| **Root** | 完整流向（含节点/通道） | 完整控制 |

## 🎯 使用场景

### 1. 流量分析
**问题**: 哪个模型消耗最多？  
**操作**: 查看流向图，线条最粗的模型即消耗最大

### 2. 成本追踪
**问题**: 某用户的成本都花在哪里？  
**操作**: (管理员) 筛选该用户，查看其 token → 模型 的流向

### 3. 路由优化
**问题**: 流量是否均衡分布到通道？  
**操作**: (Root) 查看模型 → 通道的流向，检查是否有瓶颈

### 4. 异常检测
**问题**: 发现异常大流量  
**操作**: 点击异常粗的连接，追踪源头

## ⚙️ 高级功能

### 隐藏敏感信息

点击 "眼睛" 图标，用户名/Token 名会被部分遮蔽：
- `john.doe` → `jo*****oe`
- `token-123` → `tok*****23`

### 节点筛选

使用节点筛选器只显示关心的节点类型：
- ☑ User (用户)
- ☑ Token (令牌)
- ☑ Group (用户组)
- ☑ Model (模型)
- ☑ Channel (通道)

### 时间范围

使用页面顶部的时间选择器筛选时间段：
- 建议：≤ 30 天
- 过长时间范围可能导致数据过多

## 🎨 阅读图表

### 节点大小
节点大小 = 流经该节点的总流量

### 连接粗细
连接粗细 = 该路径的流量大小

### 颜色
- 每个节点类型有固定颜色
- 高亮时使用半透明效果
- 点击节点时相关路径高亮

## 💡 最佳实践

### 1. 分析前准备
- 选择合适的时间范围（建议 7-30 天）
- 选择合适的指标（成本看 Quota，使用量看 Tokens）
- 管理员建议先看总体，再筛选特定用户

### 2. 问题排查
```
1. 发现异常 → 点击异常节点
2. 查看来源 → 追踪上游节点
3. 定位问题 → 检查配置
4. 验证修复 → 对比修复前后
```

### 3. 定期审查
- 每周查看流量分布
- 对比不同时间段
- 识别使用趋势
- 优化资源分配

## 🐛 常见问题

### Q: 为什么看不到 Flow 标签页？
A: 确保已登录，且服务器已重启加载新代码。

### Q: 图表显示空白
A: 
1. 检查时间范围内是否有数据
2. 确认权限（普通用户只看自己）
3. 检查浏览器控制台错误

### Q: 图表很复杂看不清
A:
1. 缩短时间范围
2. 使用节点筛选隐藏不关心的类型
3. 筛选特定用户（管理员）

### Q: 数据不实时更新
A: 
- Flow 图基于历史数据，非实时
- 页面会自动刷新（60秒）
- 可手动刷新页面

## 📱 移动端

功能支持移动端访问，但推荐使用桌面端以获得更好体验：
- 桌面端：完整交互功能
- 移动端：基本查看功能

## 🔗 更多资源

- **完整文档**: `SANKEY_INTEGRATION_COMPLETE.md`
- **技术分析**: `SANKEY_FEATURE_ANALYSIS.md`
- **上游 PR**: https://github.com/QuantumNous/new-api/pull/5465

---

**享受你的流量可视化之旅！** 🎉

如有问题，请查阅完整文档或提交 Issue。
